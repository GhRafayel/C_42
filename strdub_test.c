#include <string.h>
#include <unistd.h>

char *str_str(char *str)
{
    size_t len = strlen(str);

    
}

char    *foo(int a )
{
    char    *pnt;
    pnt = "hallo Warld";

    char *a = str_str(pnt);
}
int main(void)
{
    printf("%s", foo(5));
    return (0);
}